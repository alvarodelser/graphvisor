# Documents in input/ are local data (possibly confidential) and never committed.
*
!.gitignore
!README.md
!document.schema.json
# Collection builders are code, not data: keep sci_corpus's fetch script (its article list stays local).
!sci_corpus/
!sci_corpus/fetch_corpus.py
